Inheritance
    Inheriting from other classes (fulfilled by action)

Abstraction
    Abstract classes that you use to make other classes (fulfilled by ability and dice)

Polymorphism
    Using the same method name in different classes so that you can use a for loop instead of if logic (fulfilled by dice and controll gui)

Encapsulation
    Hiding information, using getters and setters (fulfilled by ability and character)